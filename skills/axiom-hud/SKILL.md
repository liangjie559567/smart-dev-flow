---
name: axiom-hud
description: Dev Flow 工作流状态仪表盘 - 显示 Axiom 状态机当前状态和阶段进度
triggers: ["/hud", "hud", "状态仪表盘", "查看进度"]
---

# axiom-hud

读取 `.agent/memory/active_context.md`，输出当前 dev-flow 工作状态仪表盘。

## 输出格式

### 1. 状态栏

```
╔══════════════════════════════════════════╗
║  Dev Flow HUD  │  {task_status}          ║
╚══════════════════════════════════════════╝
```

状态颜色规则（用 emoji 表示）：
- `IDLE` → ⚪ 空闲
- `DRAFTING` → 🔵 起草中
- `REVIEWING` → 🟡 评审中
- `DECOMPOSING` → 🟠 拆解中
- `IMPLEMENTING` → 🟢 实现中
- `REFLECTING` → 🔵 复盘中
- `BLOCKED` → 🔴 已阻塞
- `CONFIRMING` → 🟡 等待确认

### 2. 当前任务信息

```
📋 任务：{session_name 或 "无"}
📍 阶段：{current_phase 或 "—"}
🔧 子任务：{current_task 或 "—"}
⚙️  执行引擎：{execution_mode 或 "未设置"}
✅ 已完成：{completed_tasks 或 "无"}
```

若 `task_status` 为 `BLOCKED`，额外显示：
```
🚫 阻塞原因：{blocked_reason}
```

若 `fail_count > 0`，额外显示：
```
⚠️  连续失败：{fail_count} 次（≥3 次自动进入 BLOCKED）
```

### 3. 阶段进度看板

按 dev-flow 完整阶段顺序显示，当前阶段高亮（用 `▶` 标记），已完成阶段打勾：

```
Dev Flow 阶段进度
─────────────────────────────────────────
  ○ Phase 0   需求澄清
  ○ Phase 1   架构设计
  ○ Phase 1.5 专家评审
  ○ Phase 2   实现计划
  ○ Phase 3   隔离开发
  ○ Phase 4   TDD 实现
  ○ Phase 5   系统调试（按需触发）
  ○ Phase 6   代码审查
  ○ Phase 6.5 文档编写
  ○ Phase 7   完成验证
  ○ Phase 8   分支合并
  ○ Phase 9   知识收割
─────────────────────────────────────────
```

阶段状态图标：
- `○` 未开始
- `▶` 当前阶段（根据 `current_phase` 字段匹配）
- `✓` 已完成（根据 `task_status` 推断：IMPLEMENTING 表示 Phase 0-3 已完成，以此类推）

### 4. 检查点信息

若 `last_checkpoint` 非空：
```
🏁 最近检查点：{last_checkpoint}
```

### 5. 底部操作提示

根据当前 `task_status` 显示可用操作：

| 状态 | 提示 |
|------|------|
| `IDLE` | 💡 输入需求描述，或运行 `/dev-flow` 开始新任务 |
| `DRAFTING` | 💡 运行 `/dev-flow` 继续需求起草 |
| `REVIEWING` | 💡 运行 `/dev-flow` 继续专家评审 |
| `DECOMPOSING` | 💡 运行 `/dev-flow` 继续任务拆解 |
| `IMPLEMENTING` | 💡 运行 `/dev-flow` 继续实现（引擎：{execution_mode}） |
| `REFLECTING` | 💡 运行 `/dev-flow` 继续知识收割 |
| `BLOCKED` | 💡 运行 `/dev-flow` 查看恢复选项，或 `/analyze-error` 分析根因 |
| `CONFIRMING` | 💡 等待用户确认，运行 `/dev-flow` 继续 |

## 阶段推断规则

根据 `current_phase` 字段内容匹配当前阶段：

| current_phase 包含 | 当前阶段 | 已完成阶段 |
|-------------------|---------|-----------|
| `Phase 1` / `Drafting` | Phase 0+1 | — |
| `Phase 1.5` / `Reviewing` | Phase 1.5 | Phase 0, 1 |
| `Phase 2` / `Decomposing` | Phase 2 | Phase 0, 1, 1.5 |
| `Phase 3` / `Isolating` | Phase 3 | Phase 0, 1, 1.5, 2 |
| `Phase 4` / `Implementing` | Phase 4 | Phase 0-3 |
| `Phase 5` / `Debugging` | Phase 5 | Phase 0-4 |
| `Phase 6` / `Reviewing` | Phase 6 | Phase 0-5 |
| `Phase 7` / `Verifying` | Phase 7 | Phase 0-6.5 |
| `Phase 8` / `Merging` | Phase 8 | Phase 0-7 |
| `Phase 9` / `Reflecting` | Phase 9 | Phase 0-8 |
| 空 / IDLE | — | — |

## 完整输出示例

**IMPLEMENTING 状态示例：**

```
╔══════════════════════════════════════════╗
║  Dev Flow HUD  │  🟢 IMPLEMENTING        ║
╚══════════════════════════════════════════╝

📋 任务：axiom-hud-feature
📍 阶段：Phase 4 - Implementing
🔧 子任务：T2 - 编写 SKILL.md 核心逻辑
⚙️  执行引擎：standard
✅ 已完成：T1

Dev Flow 阶段进度
─────────────────────────────────────────
  ✓ Phase 0   需求澄清
  ✓ Phase 1   架构设计
  ✓ Phase 1.5 专家评审
  ✓ Phase 2   实现计划
  ✓ Phase 3   隔离开发
  ▶ Phase 4   TDD 实现
  ○ Phase 5   系统调试（按需触发）
  ○ Phase 6   代码审查
  ○ Phase 6.5 文档编写
  ○ Phase 7   完成验证
  ○ Phase 8   分支合并
  ○ Phase 9   知识收割
─────────────────────────────────────────

🏁 最近检查点：checkpoint-phase3-axiom-hud

💡 运行 /dev-flow 继续实现（引擎：standard）
```

**IDLE 状态示例：**

```
╔══════════════════════════════════════════╗
║  Dev Flow HUD  │  ⚪ IDLE                ║
╚══════════════════════════════════════════╝

📋 任务：无
📍 阶段：—
🔧 子任务：—
⚙️  执行引擎：未设置

Dev Flow 阶段进度
─────────────────────────────────────────
  ○ Phase 0   需求澄清
  ○ Phase 1   架构设计
  ○ Phase 1.5 专家评审
  ○ Phase 2   实现计划
  ○ Phase 3   隔离开发
  ○ Phase 4   TDD 实现
  ○ Phase 5   系统调试（按需触发）
  ○ Phase 6   代码审查
  ○ Phase 6.5 文档编写
  ○ Phase 7   完成验证
  ○ Phase 8   分支合并
  ○ Phase 9   知识收割
─────────────────────────────────────────

💡 输入需求描述，或运行 /dev-flow 开始新任务
```

## 触发方式

- `/hud` — 显示当前状态仪表盘
- 在任意阶段输入 "hud" 或 "查看进度" 自动触发
- `dev-flow` 技能在每次阶段切换后可选择性调用本技能刷新显示
